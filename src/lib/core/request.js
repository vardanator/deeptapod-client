const request = require('request');

const { SERVER_HOST } = require('../settings/constants');

class Request {
  setClient(clientCredentials) {
    // TODO: authorize client
  }

  makeRequest(scope, data) {
  }
}

module.exports = new Request();
